# --- FastAPI core ---
fastapi==0.111.0
uvicorn==0.30.1

# --- Pydantic & Settings ---
pydantic==2.8.2
pydantic-settings==2.3.3

# --- Database ---
sqlalchemy==2.0.31
pymysql==1.1.1
aiomysql==0.2.0  # або asyncmy==0.2.8 як альтернатива

# --- Env config (не обов'язково, якщо використовуєш pydantic-settings) ---
#python-dotenv==1.0.1

# --- Networking / HTTP ---
requests==2.32.3
aiohttp==3.9.5

# --- LDAP / WinRM / NTLM ---
ldap3==2.9.1
pywinrm==0.4.3
requests_ntlm==1.2.0
ntlm-auth==1.5.0

# --- Security / SSL ---
cryptography==42.0.8

# --- Tools / utils ---
ping3==4.0.4

# --- Development only (можна винести в dev-requirements.txt) ---
pydantic-to-typescript>=2.0.0
